# ユーザー対応が必要な作業 (USER_TODO)

Claude Codeで対応可能な作業は含めない。ユーザー本人でないと進められないもの（APIキー取得、アカウント作成、利用規約確認、VPS契約、ドメイン取得、IPアドレス等の環境固有の値の提供、手動テスト等）だけを記載する。完了した項目はこのファイルから削除する。

## 今すぐ必要

1. **本番環境（`https://novelshelf.jp`）での実機確認（推奨）**
   デプロイ完了し、実際にHTTPSで動作中です（2026-07-19、[PROGRESS.md](PROGRESS.md)参照）。特にご自身のiPhone/Androidから以下を確認いただけると安心です:
   - 縦書きの話をスマホで開いて横スクロールできるか（②の修正確認）
   - なろうの前書きがある話が正しく本文まで表示されるか（①の修正確認）
   - ノクターン/ムーンライト/ミッドナイトの作品を1件追加してみる（③の新機能確認）

2. **なろうの話本文取得方式（HTML取得）についての確認**
   なろう小説APIは作品メタデータの検索のみを提供しており、話一覧・本文は提供していないことが実装時に判明しました。そのため、話一覧・本文は`https://ncode.syosetu.com/`のページから取得する方式にしています（`robots.txt`のCrawl-delay: 1に従い1秒間隔を必ず空ける）。通常は読んでいる話を1話ずつ取得しますが、ユーザーが明示的に「全話をオフライン保存」を押した場合のみ、レート制限を維持したまま全話を順次取得します（自動での一括先読みはしません）。この方針で問題ないか確認をお願いします。懸念があれば、なろう二次利用ガイドラインを一緒に確認します。詳細は[DECISIONS.md](DECISIONS.md)の該当項目を参照してください。

3. **対応サイトの利用規約確認**（カクヨム・ハーメルン・pixiv小説）
   これら3サイトには個人開発者向けの公式APIがありません。データ取得アダプタを実装する前に、各サイトの利用規約・robots.txtを確認し、「個人利用目的でのアクセス・保存がどこまで許容されるか」を教えてください。判断が難しい場合は、Claude Codeが規約文面を一緒に確認することも可能です。（現時点ではこの3サイトのURLを登録してもリンクのみのプレースホルダーとして扱われ、本文取得は行われません。）**このユーザー確認は、検討中の「検索ページ→共有で本棚追加」機能（2026-07-19に議論、未着手）の判断にも必要**（[DECISIONS.md](DECISIONS.md)参照）。

4. **パスワードリセット機能のSMTP方式**
   実装する方針は決定済み（2026-07-19）。あとはメール送信方式をどれにするか決めてください: Gmail SMTP（アプリパスワード、個人利用なら一番手軽）/ SendGrid・AWS SES等の専用サービス / ConoHaのメールサービス。決まり次第、バックエンドのメール送信・トークン発行・フロント画面を実装します。

## 完了済み

- ~~本番デプロイ（VPS上でのclone〜TLS証明書取得〜起動確認）~~ → `https://novelshelf.jp`で稼働中。証明書自動更新のcronも設定済み（2026-07-19、[PROGRESS.md](PROGRESS.md)参照）。
- ~~GitHubリモートリポジトリの作成とpush~~ → `YUKI-1223-debug/NovelShelfApp-project`（Public）を作成しpush完了（2026-07-19）。
- ~~ドメイン取得~~ → `novelshelf.jp` を取得済み（2026-07-18）。Phase6で`.env`の`DOMAIN`・Nginx/TLS設定に反映する。
- ~~ConoHa VPSの契約とIPアドレスの共有~~ → 契約完了、IP: `163.44.116.137`（2026-07-19）。VPS初期設定（SSH鍵化・パスワード認証無効化・ファイアウォール・Docker導入・スワップ追加）まで完了。

## 将来（該当フェーズで確認予定）

- Google OAuth設定（将来のGoogleログイン追加時）
