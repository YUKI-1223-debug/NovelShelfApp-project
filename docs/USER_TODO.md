# ユーザー対応が必要な作業 (USER_TODO)

Claude Codeで対応可能な作業は含めない。ユーザー本人でないと進められないもの（APIキー取得、アカウント作成、利用規約確認、VPS契約、ドメイン取得、IPアドレス等の環境固有の値の提供、手動テスト等）だけを記載する。完了した項目はこのファイルから削除する。

## 今すぐ必要

1. **【ブロッカー】GitHubリモートリポジトリの作成とpush**
   Git設定完了、初回コミット済み（`c1fdbcd`、210ファイル）。`gh` CLIが未インストールのため、以下の手順で手動作成してください。
   1. GitHubで新しいリポジトリを作成（https://github.com/new 、Public/Privateはお好みで、README/`.gitignore`/LICENSEは追加しない＝空のリポジトリにする）
   2. リポジトリ作成後に表示されるURLを使い、以下をこのディレクトリで実行:
      ```
      git remote add origin <あなたのリポジトリのURL>
      git branch -M main
      git push -u origin main
      ```
   完了したらVPSでの`git clone`（`docs/DEPLOY.md`ステップ2以降）に進みます。

   コミット時の補足: コミット直前のレビューで、Nginx TLS動作確認用に一時生成した自己署名証明書の秘密鍵が`docs/`に紛れ込んでいるのを発見し、コミットから除外・削除しました（VPSのSSH鍵とは別物、push前だったので実害なし）。今後同様の一時ファイルを誤ってコミットしないよう`.gitignore`に`*.pem`/`*.key`を追加済みです。

2. **ブラウザでの実機確認（任意、ただし推奨）**
   `http://localhost:3002` （Docker Composeを起動したままにしています）にアクセスし、実際に使ってみてください。
   Phase5でPlaywrightによる自動E2Eテスト（サインアップ→作品追加→読書→前後の話移動→縦横書き/ダークモード切替→しおり追加→設定変更→本棚から削除、という一連の流れ）を実ブラウザで実行し、スクリーンショットでの目視確認も行い、いずれも問題ありませんでした。そのため緊急度は下がりましたが、実際に触った操作感・分かりにくい導線などは自動テストでは拾えないので、時間のあるときに試していただけると助かります。

3. **なろうの話本文取得方式（HTML取得）についての確認**
   なろう小説APIは作品メタデータの検索のみを提供しており、話一覧・本文は提供していないことが実装時に判明しました。そのため、話一覧・本文は`https://ncode.syosetu.com/`のページから取得する方式にしています（`robots.txt`のCrawl-delay: 1に従い1秒間隔を必ず空ける）。通常は読んでいる話を1話ずつ取得しますが、ユーザーが明示的に「全話をオフライン保存」を押した場合のみ、レート制限を維持したまま全話を順次取得します（自動での一括先読みはしません）。この方針で問題ないか確認をお願いします。懸念があれば、なろう二次利用ガイドラインを一緒に確認します。詳細は[DECISIONS.md](DECISIONS.md)の該当項目を参照してください。

4. **対応サイトの利用規約確認**（カクヨム・ハーメルン・pixiv小説）
   これら3サイトには個人開発者向けの公式APIがありません。データ取得アダプタを実装する前に、各サイトの利用規約・robots.txtを確認し、「個人利用目的でのアクセス・保存がどこまで許容されるか」を教えてください。判断が難しい場合は、Claude Codeが規約文面を一緒に確認することも可能です。（現時点ではこの3サイトのURLを登録してもリンクのみのプレースホルダーとして扱われ、本文取得は行われません。）

5. **パスワードリセット機能のSMTP方式**
   実装する方針は決定済み（2026-07-19）。あとはメール送信方式をどれにするか決めてください: Gmail SMTP（アプリパスワード、個人利用なら一番手軽）/ SendGrid・AWS SES等の専用サービス / ConoHaのメールサービス。決まり次第、バックエンドのメール送信・トークン発行・フロント画面を実装します。

## 完了済み

- ~~ドメイン取得~~ → `novelshelf.jp` を取得済み（2026-07-18）。Phase6で`.env`の`DOMAIN`・Nginx/TLS設定に反映する。
- ~~ConoHa VPSの契約とIPアドレスの共有~~ → 契約完了、IP: `163.44.116.137`（2026-07-19）。VPS初期設定（SSH鍵化・パスワード認証無効化・ファイアウォール・Docker導入・スワップ追加）まで完了。

## 将来（該当フェーズで確認予定）

- Google OAuth設定（将来のGoogleログイン追加時）
